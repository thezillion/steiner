cmake_minimum_required(VERSION 2.8)
project( steiner )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( steiner steiner.cpp src/combination.cpp src/reversing.cpp )
target_link_libraries( steiner ${OpenCV_LIBS} )